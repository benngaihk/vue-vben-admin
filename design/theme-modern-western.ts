/**
 * Kortex Modern Western (Linear/Vercel) Theme Configuration
 *
 * Design Philosophy: Contemporary SaaS Aesthetic
 * - Inspired by Linear, Vercel, Stripe, Notion, Raycast
 * - Refined minimalism with subtle glassmorphism
 * - Monochromatic base with strategic accent colors
 * - Premium typography (Plus Jakarta Sans / Geist)
 * - Micro-interactions that feel considered
 * - Compact, content-focused layout
 *
 * Color Psychology:
 * - Primary: Indigo Blue - Innovation, depth, sophistication
 * - Success: Emerald - Growth, verification, positive
 * - Warning: Amber - Attention without alarm
 * - Error: Rose - Modern take on error states
 */

import { defineOverridesPreferences } from '@vben/preferences';

export const overridesPreferences = defineOverridesPreferences({
  // ==========================================
  // 应用配置
  // ==========================================
  app: {
    accessMode: 'backend',
    name: import.meta.env.VITE_APP_TITLE,
    locale: 'zh-HK',
    defaultHomePath: '/welcome',
    enableRefreshToken: false,
    enableCheckUpdates: true,
    checkUpdatesInterval: 60 * 60 * 1000,
  },

  // ==========================================
  // ✨ Modern Western SaaS Theme
  // ==========================================
  theme: {
    /**
     * Refined Indigo (Linear-inspired)
     * - 现代 SaaS 最流行的主色调
     * - HSL: 239° 84% 67%
     */
    colorPrimary: 'hsl(239 84% 67%)',

    /**
     * Emerald Green
     * - 现代 SaaS 常用的成功色
     */
    colorSuccess: 'hsl(160 84% 39%)',

    /**
     * Warm Amber
     * - 温暖的琥珀警告色
     */
    colorWarning: 'hsl(45 93% 47%)',

    /**
     * Modern Rose/Coral
     * - 比纯红更现代的错误色
     */
    colorDestructive: 'hsl(350 89% 60%)',

    /**
     * 圆角配置
     * - 0.5rem = 8px，现代 SaaS 的标准圆角
     */
    radius: '0.5',

    builtinType: 'custom',

    /**
     * 半深色侧边栏
     * - 现代 SaaS 的标志性设计
     * - 无论 Light/Dark 模式，侧边栏始终深色
     */
    semiDarkSidebar: true,

    /**
     * 头部跟随主题
     * - 内容区保持与主题一致
     */
    semiDarkHeader: false,

    /**
     * 主题模式 - 自动跟随系统
     */
    mode: 'auto',
  },

  // ==========================================
  // 页脚配置 - 现代 SaaS 不显示页脚
  // ==========================================
  footer: {
    enable: false,
    fixed: false,
  },

  // ==========================================
  // 标签栏配置
  // ==========================================
  tabbar: {
    enable: true,
    persist: true, // 现代应用保持状态
    draggable: true,
    height: 38, // 紧凑设计
    showIcon: true,
    showRefresh: false, // 简洁设计，隐藏非必要按钮
    maxTabs: 8, // 保持界面整洁
  },

  // ==========================================
  // 小部件配置
  // ==========================================
  widget: {
    globalSearch: true, // Command palette 是现代标配
    notification: true,
    fullscreen: false, // 简化界面
    themeToggle: true,
    languageToggle: false, // 保持简洁
    lockScreen: false,
  },

  // ==========================================
  // 面包屑配置 - 现代 SaaS 不使用面包屑
  // ==========================================
  breadcrumb: {
    enable: false, // Linear 风格不使用面包屑
    showHome: false,
    showIcon: false,
  },

  // ==========================================
  // 导航配置 - 紧凑侧边栏模式
  // ==========================================
  navigation: {
    /**
     * 导航样式 - 侧边栏
     * - 紧凑的图标导航
     * - 类似 Linear, Notion 的设计
     */
    styleType: 'sidebar',

    /**
     * 手风琴模式
     * - 保持界面整洁
     */
    accordion: true,

    /**
     * 不分割菜单
     */
    split: false,
  },

  // ==========================================
  // 侧边栏配置 - 默认折叠，紧凑模式
  // ==========================================
  sidebar: {
    expandedWidth: 240, // 紧凑但足够
    collapsedWidth: 60, // 最小化干扰
    collapsible: true,
    collapsed: true, // 默认折叠 - 紧凑模式
    mobileCollapse: true,
  },

  // ==========================================
  // 头部配置 - 紧凑
  // ==========================================
  header: {
    height: 48, // 紧凑的 header，更多空间给内容
    enable: true,
    fixed: true,
    hidden: false,
  },

  // ==========================================
  // 过渡动画配置
  // ==========================================
  transition: {
    enable: true,
    name: 'fade', // 简洁的淡入淡出
    progress: false, // 现代应用使用更优雅的加载方式
  },

  // ==========================================
  // 快捷键配置
  // ==========================================
  shortcutKeys: {
    enable: true,
    globalSearch: {
      enable: true, // ⌘K 是现代 SaaS 标配
      key: 'k',
      modifiers: ['meta'],
    },
  },
});

/**
 * ✨ Modern Western SaaS Design Summary
 *
 * ═══════════════════════════════════════════════════════════════
 * 布局特点 - 紧凑模式 (默认折叠侧边栏)
 * ═══════════════════════════════════════════════════════════════
 *
 * 默认状态 (折叠):
 * ┌────────────────────────────────────────────────────────────┐
 * │              Header (48px)                    [🔍] [🌙] [👤] │
 * ├────┬───────────────────────────────────────────────────────┤
 * │    │                                                       │
 * │ 60 │                    Content                            │
 * │ px │                                                       │
 * │    │   ┌───────────────────────────────────────────────┐   │
 * │[🏠]│   │                                               │   │
 * │[👥]│   │              Main Content                     │   │
 * │[📊]│   │                                               │   │
 * │[⚙️]│   │         (No Breadcrumb - Clean)               │   │
 * │    │   │                                               │   │
 * │    │   └───────────────────────────────────────────────┘   │
 * │    │                                                       │
 * └────┴───────────────────────────────────────────────────────┘
 *
 * 展开状态:
 * ┌────────────────────────────────────────────────────────────┐
 * │              Header (48px)                    [🔍] [🌙] [👤] │
 * ├──────────────┬─────────────────────────────────────────────┤
 * │              │                                             │
 * │   Sidebar    │              Content                        │
 * │   (240px)    │                                             │
 * │  Semi-Dark   │   ┌─────────────────────────────────────┐   │
 * │              │   │                                     │   │
 * │  [Dashboard] │   │           Main Content              │   │
 * │  [Projects]  │   │                                     │   │
 * │  [Team]      │   │                                     │   │
 * │  [Settings]  │   │                                     │   │
 * │              │   └─────────────────────────────────────┘   │
 * │              │                                             │
 * └──────────────┴─────────────────────────────────────────────┘
 *
 * 特点:
 * - 紧凑侧边栏导航 (styleType: 'sidebar')
 * - 默认折叠 (collapsed: true) - 最大化内容区域
 * - 半深色侧边栏 (semiDarkSidebar: true)
 * - 无面包屑 (breadcrumb.enable: false)
 * - 无页脚 (footer.enable: false)
 * - 启用全局搜索 (⌘K)
 * - 紧凑 header (48px)
 * - 标签栏状态持久化 (tabbar.persist: true)
 *
 * ═══════════════════════════════════════════════════════════════
 * 色彩系统 (支持 Light / Dark 双模式)
 * ═══════════════════════════════════════════════════════════════
 *
 * 主题色:
 * ┌─────────────┬──────────────────────┬─────────┐
 * │ 角色        │ HSL                  │ HEX     │
 * ├─────────────┼──────────────────────┼─────────┤
 * │ Primary     │ hsl(239 84% 67%)     │ #6366F1 │
 * │ Success     │ hsl(160 84% 39%)     │ #10B981 │
 * │ Warning     │ hsl(45 93% 47%)      │ #F59E0B │
 * │ Destructive │ hsl(350 89% 60%)     │ #F43F5E │
 * └─────────────┴──────────────────────┴─────────┘
 *
 * Light Mode:
 * --background:           #FFFFFF
 * --background-secondary: #FAFAFA
 * --foreground:           #0F0F10
 * --foreground-muted:     #6B7280
 * --border:               #E5E7EB
 *
 * Light Mode Sidebar (Semi-Dark):
 * --sidebar-bg:           #1F2937
 * --sidebar-text:         #F9FAFB
 * --sidebar-active:       #6366F1
 *
 * Dark Mode:
 * --background:           #09090B
 * --surface:              #18181B
 * --surface-elevated:     #27272A
 * --foreground:           #FAFAFA
 * --foreground-muted:     #A1A1AA
 * --border:               #27272A
 *
 * ═══════════════════════════════════════════════════════════════
 * 设计灵感来源
 * ═══════════════════════════════════════════════════════════════
 * - Linear (任务管理) - 紧凑侧边栏，无面包屑
 * - Vercel (部署平台) - 深色侧边栏，简洁 header
 * - Stripe (支付) - 精致的微交互
 * - Notion (笔记) - 可折叠侧边栏
 * - Raycast (效率工具) - Command Palette (⌘K)
 *
 * 推荐字体:
 * --font-family: 'Plus Jakarta Sans', 'Geist', 'Inter', sans-serif;
 *
 * Glassmorphism 效果:
 * Dark: --glass-bg: rgba(255, 255, 255, 0.05);
 * Light: --glass-bg: rgba(255, 255, 255, 0.7);
 */
