/**
 * Kortex Apple Style Theme Configuration
 *
 * Design Philosophy: Apple Human Interface Guidelines
 * - Ultra-clean minimalism with purposeful whitespace
 * - SF Pro-inspired typography (using system fonts)
 * - Subtle depth through shadows, not colors
 * - Restrained color palette with blue as primary accent
 * - Precise, pixel-perfect spacing
 * - Classic sidebar navigation with generous proportions
 *
 * Color Psychology:
 * - Primary: Apple Blue - Trust, technology, innovation
 * - Success: System Green - Positive feedback, completion
 * - Warning: System Orange - Attention, caution
 * - Error: System Red - Critical, destructive actions
 */

import { defineOverridesPreferences } from '@vben/preferences';

export const overridesPreferences = defineOverridesPreferences({
  // ==========================================
  // 应用配置
  // ==========================================
  app: {
    accessMode: 'backend',
    name: import.meta.env.VITE_APP_TITLE,
    locale: 'zh-HK',
    defaultHomePath: '/welcome',
    enableRefreshToken: false,
    enableCheckUpdates: true,
    checkUpdatesInterval: 60 * 60 * 1000,
  },

  // ==========================================
  // 🍎 Apple Style Theme
  // ==========================================
  theme: {
    /**
     * Apple Blue
     * - 经典的苹果蓝，代表科技与信任
     * - HSL: 211° 100% 50%
     * - 这是 iOS/macOS 系统的标志性蓝色
     */
    colorPrimary: 'hsl(211 100% 50%)',

    /**
     * Apple Green (System Green)
     * - iOS 系统绿色，用于成功状态
     */
    colorSuccess: 'hsl(142 70% 45%)',

    /**
     * Apple Orange (System Orange)
     * - iOS 系统橙色，用于警告
     */
    colorWarning: 'hsl(28 100% 50%)',

    /**
     * Apple Red (System Red)
     * - iOS 系统红色，用于错误和删除
     */
    colorDestructive: 'hsl(0 100% 59%)',

    /**
     * 圆角配置
     * - 0.625rem = 10px，接近 iOS 的圆角风格
     */
    radius: '0.625',

    builtinType: 'custom',

    /**
     * 侧边栏跟随主题
     * - Apple 风格保持统一，侧边栏跟随 Light/Dark 模式
     */
    semiDarkSidebar: false,

    /**
     * 头部跟随主题
     * - 保持整体统一性
     */
    semiDarkHeader: false,

    /**
     * 主题模式 - 自动跟随系统
     */
    mode: 'auto',
  },

  // ==========================================
  // 页脚配置 - Apple 风格不显示页脚
  // ==========================================
  footer: {
    enable: false,
    fixed: false,
  },

  // ==========================================
  // 标签栏配置
  // ==========================================
  tabbar: {
    enable: true,
    persist: false,
    draggable: true,
    height: 40, // 舒适的触控区域
    showIcon: true,
    showRefresh: true,
    maxTabs: 12,
  },

  // ==========================================
  // 小部件配置
  // ==========================================
  widget: {
    globalSearch: false, // Apple 不强调全局搜索
    notification: true,
    fullscreen: true,
    themeToggle: true,
    languageToggle: true,
    lockScreen: true, // Apple 风格启用锁屏功能
  },

  // ==========================================
  // 面包屑配置
  // ==========================================
  breadcrumb: {
    enable: true,
    showHome: true,
    showIcon: false, // Apple 风格更简洁，不显示图标
  },

  // ==========================================
  // 导航配置 - 经典侧边栏模式
  // ==========================================
  navigation: {
    /**
     * 导航样式 - 经典侧边栏
     * - Apple 偏好传统的侧边栏导航
     * - 清晰的层级结构
     */
    styleType: 'sidebar',

    /**
     * 手风琴模式 - 开启
     * - 同时只展开一个菜单组
     * - 保持界面整洁
     */
    accordion: true,

    /**
     * 不分割菜单
     */
    split: false,
  },

  // ==========================================
  // 侧边栏配置 - 宽敞舒适
  // ==========================================
  sidebar: {
    expandedWidth: 260, // 宽敞的侧边栏
    collapsedWidth: 72,
    collapsible: true,
    collapsed: false, // 默认展开
    mobileCollapse: true,
  },

  // ==========================================
  // 头部配置
  // ==========================================
  header: {
    height: 56, // 标准 Apple 导航栏高度
    enable: true,
    fixed: true,
    hidden: false,
  },

  // ==========================================
  // 过渡动画配置
  // ==========================================
  transition: {
    enable: true,
    name: 'fade', // Apple 偏好简单的淡入淡出
    progress: true,
  },

  // ==========================================
  // 快捷键配置
  // ==========================================
  shortcutKeys: {
    enable: true,
    globalSearch: {
      enable: false,
      key: 'k',
      modifiers: ['meta'],
    },
  },
});

/**
 * 🍎 Apple Design System Summary
 *
 * ═══════════════════════════════════════════════════════════════
 * 布局特点
 * ═══════════════════════════════════════════════════════════════
 *
 * ┌────────────────────────────────────────────────────────┐
 * │                    Header (56px)                       │
 * │  [Logo]                    [🔔] [🌙] [🌐] [🔒] [👤]   │
 * ├──────────────┬─────────────────────────────────────────┤
 * │              │                                         │
 * │   Sidebar    │              Content                    │
 * │   (260px)    │                                         │
 * │              │   ┌─────────────────────────────────┐   │
 * │  [Dashboard] │   │  Breadcrumb: Home > Page        │   │
 * │  [Users]     │   ├─────────────────────────────────┤   │
 * │  [Settings]  │   │                                 │   │
 * │              │   │         Main Content            │   │
 * │              │   │                                 │   │
 * │              │   └─────────────────────────────────┘   │
 * │              │                                         │
 * └──────────────┴─────────────────────────────────────────┘
 *
 * 特点:
 * - 经典侧边栏导航 (styleType: 'sidebar')
 * - 宽敞的侧边栏 (260px)
 * - 启用锁屏功能
 * - 面包屑无图标，更简洁
 * - 不启用全局搜索
 *
 * ═══════════════════════════════════════════════════════════════
 * 色彩系统 (支持 Light / Dark 双模式)
 * ═══════════════════════════════════════════════════════════════
 *
 * 主题色:
 * ┌─────────────┬──────────────────────┬─────────┐
 * │ 角色        │ HSL                  │ HEX     │
 * ├─────────────┼──────────────────────┼─────────┤
 * │ Primary     │ hsl(211 100% 50%)    │ #007AFF │
 * │ Success     │ hsl(142 70% 45%)     │ #34C759 │
 * │ Warning     │ hsl(28 100% 50%)     │ #FF9500 │
 * │ Destructive │ hsl(0 100% 59%)      │ #FF3B30 │
 * └─────────────┴──────────────────────┴─────────┘
 *
 * Light Mode:
 * --background:          #FFFFFF
 * --background-secondary:#F5F5F7
 * --foreground:          #1D1D1F
 * --border:              #D2D2D7
 *
 * Dark Mode:
 * --background:          #000000
 * --background-elevated: #1C1C1E
 * --foreground:          #FFFFFF
 * --border:              #38383A
 *
 * Dark Mode 主题色微调:
 * --primary-dark:        #0A84FF
 * --success-dark:        #30D158
 * --warning-dark:        #FF9F0A
 * --destructive-dark:    #FF453A
 */
