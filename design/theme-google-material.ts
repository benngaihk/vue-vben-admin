/**
 * Kortex Google Material Design 3 Theme Configuration
 *
 * Design Philosophy: Material Design 3 (Material You)
 * - Dynamic color system with tonal palettes
 * - Expressive, friendly, and accessible
 * - Rounded shapes with emphasis on usability
 * - Bold typography with clear hierarchy
 * - Motion that feels natural and responsive
 * - Mixed navigation with top menu + side drawer
 *
 * Color Psychology:
 * - Primary: Google Blue - Dependable, intelligent, clear
 * - Success: Tonal Green - Growth, positivity, confirmation
 * - Warning: Tonal Amber - Alert, attention, caution
 * - Error: Tonal Red - Critical, urgent, destructive
 */

import { defineOverridesPreferences } from '@vben/preferences';

export const overridesPreferences = defineOverridesPreferences({
  // ==========================================
  // 应用配置
  // ==========================================
  app: {
    accessMode: 'backend',
    name: import.meta.env.VITE_APP_TITLE,
    locale: 'zh-HK',
    defaultHomePath: '/welcome',
    enableRefreshToken: false,
    enableCheckUpdates: true,
    checkUpdatesInterval: 60 * 60 * 1000,
  },

  // ==========================================
  // 🎨 Google Material Design 3 Theme
  // ==========================================
  theme: {
    /**
     * Google Blue (Material Primary)
     * - Material Design 3 的标志性蓝色
     * - HSL: 217° 89% 61%
     */
    colorPrimary: 'hsl(217 89% 61%)',

    /**
     * Material Tonal Green
     * - MD3 风格的成功色
     */
    colorSuccess: 'hsl(152 60% 42%)',

    /**
     * Material Tonal Amber
     * - MD3 风格的警告色
     */
    colorWarning: 'hsl(43 96% 56%)',

    /**
     * Material Tonal Red
     * - MD3 风格的错误色
     */
    colorDestructive: 'hsl(3 74% 57%)',

    /**
     * 圆角配置
     * - 0.75rem = 12px，符合 MD3 的 "Full" 圆角风格
     */
    radius: '0.75',

    builtinType: 'custom',

    /**
     * 侧边栏跟随主题
     * - Material 3 保持侧边栏与主题一致
     */
    semiDarkSidebar: false,

    /**
     * 半深色头部
     * - Google 风格的深色顶栏，增强视觉层次
     */
    semiDarkHeader: true,

    /**
     * 主题模式 - 自动跟随系统
     */
    mode: 'auto',
  },

  // ==========================================
  // 页脚配置 - Google 风格启用固定页脚
  // ==========================================
  footer: {
    enable: true,
    fixed: true, // 固定在底部
  },

  // ==========================================
  // 标签栏配置
  // ==========================================
  tabbar: {
    enable: true,
    persist: false,
    draggable: true,
    height: 42, // Material 推荐更大的触控区域
    showIcon: true,
    showRefresh: true,
    maxTabs: 10,
  },

  // ==========================================
  // 小部件配置
  // ==========================================
  widget: {
    globalSearch: true, // Google 强调搜索功能
    notification: true,
    fullscreen: true,
    themeToggle: true,
    languageToggle: true,
    lockScreen: false,
  },

  // ==========================================
  // 面包屑配置
  // ==========================================
  breadcrumb: {
    enable: true,
    showHome: true,
    showIcon: true, // Material 喜欢使用图标增强可读性
  },

  // ==========================================
  // 导航配置 - 混合模式 (顶部 + 侧边)
  // ==========================================
  navigation: {
    /**
     * 导航样式 - 混合模式
     * - 顶部导航栏显示一级菜单
     * - 侧边栏显示二级及以下菜单
     * - 类似 Google Cloud Console 的布局
     */
    styleType: 'mixed',

    /**
     * 允许多个菜单组同时展开
     * - Material 风格更开放
     */
    accordion: false,

    /**
     * 分割菜单
     * - 一级菜单在顶部，子菜单在侧边
     */
    split: true,
  },

  // ==========================================
  // 侧边栏配置
  // ==========================================
  sidebar: {
    expandedWidth: 256, // Material 标准导航抽屉宽度
    collapsedWidth: 80, // 足够显示图标和 tooltip
    collapsible: true,
    collapsed: false, // 默认展开
    mobileCollapse: true,
  },

  // ==========================================
  // 头部配置
  // ==========================================
  header: {
    height: 64, // Material Design 推荐的 App Bar 高度
    enable: true,
    fixed: true,
    hidden: false,
  },

  // ==========================================
  // 过渡动画配置
  // ==========================================
  transition: {
    enable: true,
    name: 'fade-slide', // Material 喜欢有方向感的动画
    progress: true,
  },

  // ==========================================
  // 快捷键配置
  // ==========================================
  shortcutKeys: {
    enable: true,
    globalSearch: {
      enable: true, // Google 产品强调搜索
      key: 'k',
      modifiers: ['meta'],
    },
  },
});

/**
 * 🎨 Google Material Design 3 Summary
 *
 * ═══════════════════════════════════════════════════════════════
 * 布局特点 - 混合导航模式
 * ═══════════════════════════════════════════════════════════════
 *
 * ┌────────────────────────────────────────────────────────────┐
 * │              Header (64px) - Semi-Dark                     │
 * │  [Logo] [Home] [Users] [Reports] [Settings]    [🔍] [👤]  │
 * ├──────────────┬─────────────────────────────────────────────┤
 * │              │                                             │
 * │   Sidebar    │              Content                        │
 * │   (256px)    │                                             │
 * │   子菜单      │   ┌─────────────────────────────────────┐   │
 * │              │   │  🏠 Breadcrumb: Home > Users > List  │   │
 * │  [All Users] │   ├─────────────────────────────────────┤   │
 * │  [Admins]    │   │                                     │   │
 * │  [Groups]    │   │           Main Content              │   │
 * │  [Roles]     │   │                                     │   │
 * │              │   └─────────────────────────────────────┘   │
 * │              │                                             │
 * ├──────────────┴─────────────────────────────────────────────┤
 * │                    Footer (Fixed)                          │
 * │              © 2024 Kortex. All rights reserved.           │
 * └────────────────────────────────────────────────────────────┘
 *
 * 特点:
 * - 混合导航模式 (styleType: 'mixed')
 * - 顶部显示一级菜单，侧边显示子菜单 (split: true)
 * - 半深色头部 (semiDarkHeader: true)
 * - 启用固定页脚 (footer.enable: true, footer.fixed: true)
 * - 面包屑带图标
 * - 启用全局搜索 (⌘K)
 * - 允许多个菜单同时展开 (accordion: false)
 *
 * ═══════════════════════════════════════════════════════════════
 * 色彩系统 (支持 Light / Dark 双模式)
 * ═══════════════════════════════════════════════════════════════
 *
 * 主题色:
 * ┌─────────────┬──────────────────────┬─────────┐
 * │ 角色        │ HSL                  │ HEX     │
 * ├─────────────┼──────────────────────┼─────────┤
 * │ Primary     │ hsl(217 89% 61%)     │ #4285F4 │
 * │ Success     │ hsl(152 60% 42%)     │ #2BA568 │
 * │ Warning     │ hsl(43 96% 56%)      │ #FBBC04 │
 * │ Destructive │ hsl(3 74% 57%)       │ #EA4335 │
 * └─────────────┴──────────────────────┴─────────┘
 *
 * Light Mode:
 * --background:           #FFFBFE
 * --surface:              #FFFBFE
 * --surface-container:    #F3EDF7
 * --on-surface:           #1C1B1F
 * --outline:              #79747E
 *
 * Dark Mode:
 * --background:           #1C1B1F
 * --surface:              #1C1B1F
 * --surface-container:    #211F26
 * --on-surface:           #E6E1E5
 * --outline:              #938F99
 *
 * Dark Mode 主题色 (Tonal Palette):
 * --primary-dark:         #A8C7FA
 * --success-dark:         #6DD58C
 * --warning-dark:         #ECD271
 * --destructive-dark:     #F2B8B5
 *
 * ═══════════════════════════════════════════════════════════════
 * Material 3 特色
 * ═══════════════════════════════════════════════════════════════
 * - 动态颜色 (Dynamic Color)
 * - 色调调色板 (Tonal Palettes)
 * - 表达性容器 (Expressive Containers)
 * - Elevation 阴影系统 (0-5级)
 *
 * 推荐字体:
 * --font-family: 'Google Sans', 'Roboto', 'Noto Sans SC', sans-serif;
 */
